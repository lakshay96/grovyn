# Grovyn web — environment
# The app falls back to bundled mock data if the API is unreachable,
# so it runs fully even with NO backend.

NEXT_PUBLIC_API_URL=http://localhost:5050/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
